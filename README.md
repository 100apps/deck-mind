# 🧠 Deck Mind (斗地主记牌小神童)

> **三天练成“最强大脑”，辅助你达到极高的记牌水平。**

**Deck Mind** 是一个专为斗地主爱好者设计的**React 单页应用 (SPA)**。它不仅仅是一个简单的打牌游戏，更是一个**深度记忆力训练工具**。通过视觉化的辅助（“小火车”记牌器）和强制性的记忆回溯（随机抽查），帮助用户在实战模拟中快速掌握记牌技巧。

🔗 **在线体验**: [https://deck-mind.gf.sh.cn](https://deck-mind.gf.sh.cn)

---

## 🎯 项目目的 (Purpose)

大多数斗地主玩家输牌的原因并非牌技不精，而是**记不住牌**。本项目旨在解决这一痛点：
1.  **脱离枯燥记忆**：通过游戏化的方式，让记牌变得直观。
2.  **强化残局感知**：训练用户对“外面还剩什么牌”的敏锐直觉。
3.  **三天速成**：通过高强度的视觉刺激和即时反馈，短期内提升记忆潜能。

## 💡 训练策略 (Training Strategy)

本应用采用了以下核心策略来辅助训练：

### 1. 视觉化记忆锚点 - "小火车"记牌器
*   **设计理念**：将剩余的牌具象化为一列火车的车厢。
*   **颜色编码**：
    *   🟩 **翠绿色**：我出的牌（安全）。
    *   🟦 **湛蓝色**：上家（老赢/爸爸）出的牌。
    *   🟥 **玫红色**：下家（老输/妈妈）出的牌。
*   **作用**：用户不再需要在大脑中构建抽象的数字列表，而是通过视觉残留（Visual Persistence）记住颜色的分布。

### 2. 强制性主动回忆 (Active Recall)
*   **随机抽查**：游戏过程中会随机暂停（Trigger），弹出“随机测验模态框”。
*   **核心问题**：“外面还剩几张 K？”、“大王出了没？”。
*   **即时反馈**：答对加分，答错立即显示正确答案，强化记忆修正。

### 3. 沉浸式家庭模拟
*   **TTS 语音合成**：利用 Web Speech API，为三位玩家配置了不同音色（活泼的“我”、低沉的“爸爸”、温和的“妈妈”），增强听觉记忆辅助。
*   **真实头像**：支持加载本地真实照片，模拟家庭对战氛围。

---

## 🏗️ 技术架构 (Architecture)

本项目基于现代前端工程化标准构建，追求高性能与低延迟。

### 技术栈
*   **核心框架**: [React 18](https://react.dev/) (Hooks, Functional Components)
*   **构建工具**: [Vite](https://vitejs.dev/) (极速 HMR 与构建)
*   **语言**: [TypeScript](https://www.typescriptlang.org/) (严格类型检查)
*   **样式**: [Tailwind CSS](https://tailwindcss.com/) (原子化 CSS)
*   **图标**: [Lucide React](https://lucide.dev/)
*   **部署**: GitHub Actions + GitHub Pages

### 目录结构

```
.
├── .github/workflows/   # CI/CD 自动化部署配置
├── public/              # 静态资源 (photo/ 头像目录)
├── src/
│   ├── components/      # UI 组件
│   │   ├── PlayingCard.tsx    # 扑克牌渲染组件
│   │   ├── RecorderTrain.tsx  # 核心记牌器（小火车）
│   │   ├── QuizModal.tsx      # 记忆抽查弹窗
│   │   └── PlayerSpot.tsx     # 玩家席位与头像
│   ├── utils/           # 核心逻辑
│   │   ├── cardLogic.ts       # 扑克牌算法（发牌、牌型判断、AI出牌）
│   │   └── soundUtils.ts      # 语音合成 (TTS) 封装
│   ├── types.ts         # TypeScript 类型定义
│   ├── App.tsx          # 游戏主循环与状态管理
│   └── index.tsx        # 入口文件
├── index.html           # HTML 模板
├── package.json         # 依赖管理
├── tsconfig.json        # TS 配置
└── vite.config.ts       # 构建配置
```

### 核心逻辑实现方案

1.  **状态管理 (State Management)**:
    *   使用 React `useState` 和 `useReducer` 思想管理全局状态：`players` (手牌), `currentTableHand` (桌面牌), `remainingCounts` (记牌器数据), `playedCardHistory` (出牌历史)。
    
2.  **出牌算法 (Bot Logic)**:
    *   在 `cardLogic.ts` 中实现了一个基于规则的简易 AI。
    *   **策略**：优先管牌 -> 优先出多张牌型（顺子/三带一） -> 优先出小牌。
    *   虽然不是最强 AI，但足够作为陪练，让用户专注于记牌。

3.  **自动化部署 (CI/CD)**:
    *   配置了 GitHub Actions (`deploy.yml`)。
    *   每当代码 Push 到主分支，自动执行 `npm run build`，并在 `dist` 目录生成 `CNAME` 文件，然后将产物部署到 GitHub Pages。

---

## 🚀 快速开始 (Getting Started)

### 本地开发

1.  **克隆仓库**:
    ```bash
    git clone https://github.com/100apps/deck-mind.git
    cd deck-mind
    ```

2.  **安装依赖**:
    ```bash
    npm install
    ```

3.  **启动开发服务器**:
    ```bash
    npm run dev
    ```
    打开浏览器访问 `http://localhost:5173`。

### 自定义配置

*   **修改头像**: 将你的家庭成员照片放入 `public/photo/` 目录，并分别命名为 `xiaoying.jpg`, `laoying.jpg`, `laoshu.jpg`。
*   **调整 AI 速度**: 在 `App.tsx` 中修改 `autoPlaySpeed` (默认 1500ms)。

---

## 📦 部署 (Deployment)

本项目已配置自动化部署。

1.  进入 GitHub 仓库的 **Settings** -> **Pages**。
2.  在 **Build and deployment** 下，将 **Source** 设置为 **GitHub Actions**。
3.  在 **Custom domain** 设置中，输入 `deck-mind.gf.sh.cn` (如果需要手动验证 DNS，请参照 GitHub 文档)。
4.  Push 代码，等待 Action 执行完毕。
5.  访问 `https://deck-mind.gf.sh.cn`。

---

## 🤝 贡献 (Contributing)

欢迎提交 Issue 或 Pull Request 来改进记牌逻辑或 UI 体验！

## 📄 License

MIT License